import 'package:flutter/material.dart';

class PreLogueoPage extends StatelessWidget {
  const PreLogueoPage({Key key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container(
      decoration: BoxDecoration(
        color: Colors.white
      ),
      child: Text("Prelogueo"),
    );
  }
}